# Root optional secrets for tooling / docker compose interpolation.
# Copy values into a local .env — never commit real tokens.

HF_TOKEN=
POSTGRES_PASSWORD=CHANGE_ME_DB_PASSWORD
KEYCLOAK_ADMIN_PASSWORD=CHANGE_ME_KEYCLOAK_PASSWORD
